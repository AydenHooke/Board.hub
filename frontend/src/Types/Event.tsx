export interface Event{
    accountId: number | null,
    content: string | null,
    dateCreated: Date | null,
    dateMeet: Date | null,
    eventId: number | null,
    gameId: number | null,
    status: string | null,
    title: string | null,
    type: string | null,
    username: string | null 
}

